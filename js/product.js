const money=n=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',maximumFractionDigits:0}).format(n);const id=Number(new URLSearchParams(location.search).get('id'))||1;const p=products.find(x=>x.id===id)||products[0];const $=s=>document.querySelector(s);function render(){document.title=`EAZY — ${p.name}`;$('#crumbName').textContent=p.name;$('#productName').textContent=p.name;$('#rating').textContent=`★ ${p.rating}`;$('#reviewsLink').textContent=`${p.reviews} reviews`;$(`#sold`).textContent=`${(p.reviews/500).toFixed(1)}k sold`;$(`#price`).textContent=money(p.price);$('#oldPrice').textContent=money(p.oldPrice);$('#saveBadge').textContent=`Save ${p.discount||0}%`;$('#imageBadge').textContent=p.discount?`-${p.discount}%`:'EAZY PICK';$('#description').textContent=p.description||'A great product selected for EAZY shoppers. Enjoy a clear, easy shopping experience with product information, pricing and support in one place.';$('#mainEmoji').textContent=p.emoji;const imgs=[p.emoji,'📦','✨','🔍'];$('#thumbs').innerHTML=imgs.map((x,i)=>`<button class="thumb ${i===0?'active':''}" data-img="${x}">${x}</button>`).join('');$('#relatedProducts').innerHTML=products.filter(x=>x.id!==p.id).slice(0,4).map(card).join('')}function card(x){return `<article class="product-card"><a href="product.html?id=${x.id}" style="text-decoration:none;color:inherit"><div class="product-image">${x.discount?`<span class="discount">-${x.discount}%</span>`:''}<button class="heart" onclick="event.preventDefault()">♡</button><span>${x.emoji}</span></div><div class="product-info"><div class="product-name">${x.name}</div><div class="rating"><strong>★ ${x.rating}</strong> · ${x.reviews} reviews</div><div class="price-row"><span class="price">${money(x.price)}</span><span class="old-price">${money(x.oldPrice)}</span></div></div></a></article>`}let qty=1;$('#plus').onclick=()=>{$('#qty').textContent=++qty};$('#minus').onclick=()=>{if(qty>1)$('#qty').textContent=--qty};document.addEventListener('click',e=>{const t=e.target.closest('.thumb');if(t){document.querySelectorAll('.thumb').forEach(x=>x.classList.remove('active'));t.classList.add('active');$('#mainEmoji').textContent=t.dataset.img}});document.querySelectorAll('.choice').forEach(b=>b.onclick=()=>{document.querySelectorAll('.choice').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')});function toast(msg){const t=$('#toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}$('#addCart').onclick=()=>toast(`${qty} × ${p.name} added to cart`);$('#buyNow').onclick=()=>toast('Ready for checkout');render();